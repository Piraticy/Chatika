{
  "name": "chatika-web",
  "private": true,
  "version": "0.4.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:render": "npm run build && mkdir -p ../backend/web_dist && cp -R dist/. ../backend/web_dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
